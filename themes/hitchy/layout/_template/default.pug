extends ../_partial/document

block title
	= page.title + " | " + config.title

block north-before
	nav.primary
		include ../_partial/top-menu.pug

block north
	.block.boilerplate
		h1 #{config.title}
		h2 #{config.subtitle}
